body {
  margin: 0;
  font-family: sans-serif;
  background-color: #000;
  color: #fff;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 90vh;
  flex-direction: column;
  position: relative;
}

.container { width: 100%; max-width: 400px; border-radius: 10px; overflow: hidden; position: relative; }
.background-image { background-image: url('background.gif'); background-size: cover; background-position: center; height: 200px; }

.profile-section { padding: 20px; text-align: center; position: relative; }
.profile-picture-wrapper { position: relative; width: 100px; height: 100px; margin: -50px auto 0; }
.profile-picture { width: 100%; height: 100%; border-radius: 50%; object-fit: cover; border: 2px solid #fff; background-color: #000; }
.profile-decoration { position: absolute; top: -10px; left: 50%; width: 120%; transform: translateX(-50%); pointer-events: none; }

h2 { margin: 10px 0 5px; font-size: 1.5em; }
.username { color: #aaa; margin-bottom: 15px; }

.social-links { display: flex; justify-content: center; }
.social-links a { margin: 0 10px; }
.social-links img { width: 30px; height: 30px; }

.visitor-count {
  position: fixed;
  left: 10px;
  bottom: 10px;
  display: flex;
  align-items: center;
  z-index: 1000;
  padding: 5px 10px;
  border: 2px solid #333;
  border-radius: 8px;
  background-color: #111;
  gap: 5px;
  cursor: pointer;
}
.views-label { color: #ccc; font-size: 13px; user-select: none; }

.eye-icon { width: 20px; height: 20px; margin-right: 5px; }

/* ===== أزرار الزاوية ===== */
.corner-buttons {
  position: fixed;
  top: 10px;
  right: 10px;
  display: flex;
  gap: 10px;
  z-index: 2000;
  flex-direction: row-reverse; /* السر أقصى الزاوية */
}

.secret-btn, .about-btn {
  width: 42px;
  height: 42px;
  border: 2px solid #777;
  border-radius: 10px;
  background-color: #222;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: transform 0.2s ease;
}

.secret-btn img, .about-btn img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.secret-btn:hover, .about-btn:hover { transform: scale(1.1); }

/* ===== مودال المعلومات ===== */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.5);
  backdrop-filter: blur(4px);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 2500;
}

.modal-box.about-modal {
  background: #111;
  padding: 20px 25px;
  border-radius: 14px;
  width: 300px;
  max-width: 90%;
  display: flex;
  flex-direction: column;
  gap: 10px;
  position: relative;
  animation: modalFade 0.25s ease-in-out;
}

.modal-box.about-modal h3 {
  margin: 0;
  text-align: center;
  color: #fff;
}

.modal-box.about-modal p {
  margin: 2px 0;
  color: #ccc;
  font-size: 14px;
}

.close-about {
  position: absolute;
  top: 8px;
  right: 10px;
  background: none;
  border: none;
  color: #fff;
  font-size: 16px;
  cursor: pointer;
}

/* ===== أنيميشن سلس ===== */
@keyframes modalFade {
  from { transform: scale(0.9); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

/* مودال كلمة السر والفويس */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(6px);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 2000;
}

.modal-box {
  background: #111;
  border-radius: 14px;
  padding: 20px;
  max-width: 350px;
  width: 90%;
  display: flex;
  flex-direction: column;
  gap: 10px;
  align-items: center;
}

.password-modal p,
.about-modal p,
.about-modal h3 {
  color: #ccc;
  font-size: 16px;
  text-align: center;
}

.password-modal input {
  width: 80%;
  padding: 8px;
  border-radius: 8px;
  border: 2px solid #555;
  background: #222;
  color: #fff;
  font-size: 14px;
  text-align: center;
}

.password-buttons {
  display: flex;
  gap: 10px;
}

.password-buttons button,
.about-modal .close-about,
#playPauseVoice,
#closeVoice {
  background: #222;
  border: none;
  color: #fff;
  border-radius: 6px;
  padding: 6px 12px;
  cursor: pointer;
}

/* مودال الفويس */
.voice-modal { max-width: 400px; }
.voice-box { display: flex; align-items: center; gap: 15px; width: 100%; }
.voice-img { width: 70px; height: 70px; object-fit: cover; border-radius: 8px; flex-shrink: 0; }
.voice-controls { flex: 1; display: flex; flex-direction: column; gap: 8px; }
#voice-time { font-size: 14px; color: #ccc; text-align: left; }
.voice-progress-bar { width: 100%; height: 6px; background: #333; border-radius: 3px; position: relative; }
.voice-progress-fill { height: 100%; width: 0%; background: #aaa; border-radius: 3px; }
.voice-thumb { width: 12px; height: 12px; background: #fff; border-radius: 50%; position: absolute; top: 50%; transform: translate(-50%, -50%); cursor: pointer; }
.voice-buttons { display: flex; gap: 10px; margin-top: 8px; }

/* مودال معلومات عني */
.about-modal {
  max-width: 300px;
  background: #111;
  border: 2px solid #555;
  text-align: center;
  position: relative;
}

.about-modal h3 { margin-bottom: 10px; }
.about-modal p { margin: 5px 0; }
.about-modal .close-about {
  position: absolute;
  top: 5px;
  right: 8px;
  font-size: 18px;
}

/* Music Box */
.music-box { position: absolute; top: 10px; left: 10px; width: 70px; height: 70px; border: 3px solid #888; border-radius: 12px; overflow: hidden; cursor: pointer; z-index: 100; background-color: #313131; }
.music-box img { width: 100%; height: 100%; object-fit: cover; }

/* Song List */
.song-list {
  position: absolute;
  top: 90px;
  left: 10px;
  background-color: #111;
  border-radius: 10px;
  display: none;
  flex-direction: column;
  width: 300px;
  z-index: 99;
  padding: 5px;
  border: 3px solid;
  animation: rgb-border 3s linear infinite;
}

@keyframes rgb-border {
  0% { border-color: #191919; }
  33% { border-color: #545454; }
  66% { border-color: #434343; }
  100% { border-color: #191919 }
}

.song { padding: 10px; border-bottom: 1px solid #333; margin-bottom: 5px; }
.song:last-child { border-bottom: none; }
.song-info { display: flex; align-items: center; gap: 10px; margin-bottom: 5px; }
.song-info img { width: 45px; height: 45px; border-radius: 8px; object-fit: cover; }
.song-info span { font-size: 15px; color: #fff; }
.song-controls { display: flex; justify-content: space-between; align-items: center; gap: 5px; margin-bottom: 5px; }
.song-controls button { background: #000; color: #fff; font-size: 18px; border: none; cursor: pointer; border-radius: 5px; padding: 4px 6px; }
.song-controls span.song-time { font-size: 12px; color: #ccc; }
.progress-bar { width: 100%; height: 6px; background-color: #333; border-radius: 3px; overflow: hidden; position: relative; }
.progress-fill { height: 100%; width: 0%; background: #aaa; border-radius: 3px; }
.progress-thumb { width: 12px; height: 12px; background-color: #fff; border-radius: 50%; position: absolute; top: 50%; transform: translate(-50%, -50%); cursor: pointer; }
